# URL base do koin-api (sem /api/v1).
# Em desenvolvimento pode ficar vazio: o app usa o IP da máquina que roda o Metro na porta 8000,
# o que funciona no celular físico (Expo Go) e no emulador.
# Android emulator com API em localhost: http://10.0.2.2:8000
EXPO_PUBLIC_API_URL=
